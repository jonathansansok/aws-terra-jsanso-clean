# =========================================================
# Repo-safe .gitignore
# Goal: keep the repo runnable for reviewers, never leak secrets
# =========================================================

# -------------------------
# OS / Editor
# -------------------------
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# -------------------------
# Node / JS / TS
# -------------------------
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Caches / tooling
.cache/
.parcel-cache/
.eslintcache
*.tsbuildinfo
coverage/

# Builds
dist/
build/
out/
.next/
.vercel/
.turbo/

# -------------------------
# Env / Secrets
# -------------------------
# Ignore all env files, but keep template examples
.env
.env.*
!.env.example
!.env.local.example
!.env.production.example

# -------------------------
# Terraform (safe for GitHub)
# -------------------------
# Never commit state or local terraform dirs
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfplan

# Local terraform CLI config
.terraformrc
terraform.rc

# Keep tfvars examples committed, ignore real tfvars
infra/envs/*.tfvars
!infra/envs/*.tfvars.example

# IMPORTANT:
# Do NOT ignore .terraform.lock.hcl (should be committed for reproducibility)
# (No rule here on purpose)

# -------------------------
# Keys / Certs / Credentials
# -------------------------
# Generic key material
keys/
*.pem
*.ppk
*.key
*.p12
*.pfx

# Cloud credentials / local auth
**/credentials.*
**/*_credentials.*
**/.aws/
**/.gcp/
**/service-account*.json

# SSH private keys by common names
id_rsa
id_ed25519
id_ecdsa
*.pub
known_hosts

# -------------------------
# Local debug / bundles / dumps
# -------------------------
bundle_debug.md
bundle_debug_*.md
*.dump
*.bak
*.tmp
